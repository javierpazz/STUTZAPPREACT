import React from 'react';
import AppOrd from './components/AppOrd';
//import AuthContext from "./context/auth"

export default function InvoicesOrd() {
  //  const { user } = useContext(AuthContext)

  return (
    <>
      <AppOrd />
    </>
  );
}
